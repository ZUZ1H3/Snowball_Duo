import javax.swing.*;

public class GameServerFrame extends JFrame {
    public GameServerFrame(int port){
        setTitle("Snowball Duo - Server");
        setSize(1000, 600);
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        setVisible(true);

    }
}
